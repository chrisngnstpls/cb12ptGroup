module.exports = {
    
    exits: {
        success: {
            viewTemplatePath: 'pages/experimental/geolocate' // default exit view to signup form
        }
    },

    fn: async function() {
        
        return {}
    }
}