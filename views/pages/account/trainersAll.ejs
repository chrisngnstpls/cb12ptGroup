<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WTF</title>
</head>
<html>
<h1>List of Trainers</h1>

<div id="products" class="container">
  <table class="table table-striped">
    <thead class="thead-light">
      <th>Id</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Date of birth</th>
      <th>Bio</th> 
      <th colspan="2">Actions</th>
    </thead>
    <tbody>
      <% 
        var bookUrl = "book"; 

      %>
    <% for (let i = 0; i < data.length; i++) { %>
        <% 
            var myBook = "/trainers/" + bookUrl + "/" + data[i].id;

        %>
      
          <tr>
            <td><%=data[i].id%></td>
            <td><%=data[i].fname%></td>
            <td><%=data[i].lname%></td>
            <td><%=new Date(data[i].birthDate).toLocaleString()%></td>
            <td><%=data[i].bio%></td>
            <td><a href="<%=myBook%>">Book now</a></td>
          </tr>   
    <% } %>
  </tbody>
</table>
</div>

</html>